/**
 * Class strings shared by more than one section, kept in one place so the
 * Skills / Projects / Contact shells can't drift apart.
 */

/** Outer shell for the three white content sections beneath About. */
export const SECTION_SHELL =
  "relative flex w-full flex-col items-center justify-center overflow-hidden border-t border-gray-100 bg-white px-6 py-16 text-gray-900 md:px-16 lg:px-24";

/** Text input / textarea chrome used by the contact form. */
export const FIELD_CONTROL =
  "w-full rounded-xl border border-gray-200 bg-white px-4 py-3 text-sm font-medium transition-all focus:border-spidey focus:ring-1 focus:ring-spidey focus:outline-none";
